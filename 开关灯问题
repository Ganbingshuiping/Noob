/*有n盏灯，编号为1—n，第1个人把所有灯打开，第2个人按下所有编号为2的倍数的开关（这些灯将被关掉），
第三个人按下所有编号为3的倍数的开关（其中关掉的灯将被打开，开着的灯将被关闭），
以此类推，一共有k个人，编号为1—k。请问最后有哪些灯是开着的？

设计要求：

（1）输入灯的个数n和人数k

（2）输出最后开着的灯的编号。

（3）k<=n<=100*/
#include<stdio.h>
int main()
{
    int n,k,i,arr[100]={0};//灯数n,人数k，第(1)要求
        printf("输入灯的数量n：");
        scanf("%d",&n);
        printf("输入人的数量k：");
        scanf("%d",&k);
        if(n>100||k>100)//第(3)要求
            printf("您输入的数字过大");
        else
        {
            printf("开着的灯：");
            for(;2<=k;k--)//由于第一个人将灯全打开，故设定0为亮1为灭
            {
                for(i=1;(i*k)<=n;i++)//i*k判断第几个人关他自己倍数的灯
                {   //进行灯的开关判断，元素角标与指定的要求差1
                    if(arr[i*k-1]==0)
                        arr[i*k-1]=1;
                    else
                        arr[i*k-1]=0;
                }
            }
            for(i=0;i<n;i++)//第(2)要求
            {
                if(arr[i]==0)
                printf("%d ",i+1);
            }
        }
    return 0;
}
